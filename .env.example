# ── Environment ──────────────────────────────────
APP_ENV=local                    # local | staging | prod
DOMAIN=blogwriter.test           # Public domain (used by proxy + SSL)

# ── Docker ───────────────────────────────────────
CONTAINER_PREFIX=blogwriter

# ── Database ─────────────────────────────────────
POSTGRES_USER=blogwriter_user
POSTGRES_PASSWORD=blogwriter_pass
POSTGRES_DB=blogwriter_db
# DATABASE_URL=                  # Override for external database

# ── API Keys ─────────────────────────────────────
ADMIN_TOKEN=dev-admin-token      # Change for non-local!
OPENROUTER_API_KEY=

# ── Next.js (build-time) ────────────────────────
# Where the browser makes API calls (through proxy in prod)
NEXT_PUBLIC_API_URL=http://blogwriter.test:4444

# ── CORS ─────────────────────────────────────────
# CORS_ORIGIN=https://yourdomain.com   # Restrict in production

# ── Ports (local dev only) ──────────────────────
PROXY_PORT=8081
PROXY_SSL_PORT=8444
API_EXTERNAL_PORT=4444
NEXTJS_EXTERNAL_PORT=4443

# ── SSL ──────────────────────────────────────────
CERT_EMAIL=admin@example.com     # Email for Let's Encrypt registration

# ── Docker Compose Profiles ─────────────────────
# db    = run Postgres/Valkey containers (remove for external services)
# certbot = only used by ./cli certs
COMPOSE_PROFILES=db
